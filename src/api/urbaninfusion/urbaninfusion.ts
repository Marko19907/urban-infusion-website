// TODO Girts - I would probably define it as a constant (API_BASE_URL), not function. But very good idea to define a global constant!
export function getPath() {
    return `http://10.212.26.22:8080`;
}
